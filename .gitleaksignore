# gitleaks allowlist — one fingerprint per line.
# Fingerprint format: <commit-sha>:<file-path>:<rule-id>:<line-number>
# Generate by running gitleaks locally; copy the fingerprint from output.
#
# Example (uncomment and replace with real findings):
# a1b2c3d4e5f6...:src/config/test-fixtures.ts:generic-api-key:42
